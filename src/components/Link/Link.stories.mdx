import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import Link from './Link';

<Meta
  title="Design System/Link/Overview"
  component={Link}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true }
    }
  }}
/>

## Link

The Link component can be used to customize anchor elements.

It uses the `Typography` component to render the Link text, and accepts any of its props.

<Canvas>
  <Story id="design-system-link--default" />
</Canvas>

<br />

## Available props

<ArgsTable of={Link} />
